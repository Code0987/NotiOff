name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout
    - uses: Code0987/android-ci-github-action
      args: ./src/gradlew -p ./src check
